# Tylko do `astro dev`: adres peera (w kontenerze strona i peer są na jednym hoście).
PUBLIC_PEER_URL=http://localhost:8080
# Kanoniczny adres strony (sitemap/canonical).
SITE_URL=http://localhost:8080
